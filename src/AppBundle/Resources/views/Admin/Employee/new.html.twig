{% extends '::base.html.twig' %}

{% block body %}
    <div class="container new-employee-container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="well well-sm">
                    <h1 class="text-primary text-center">Добавление нового сотрудника</h1>
                </div>

                {{ form_start(form) }}
                {{ form_widget(form) }}

                <input type="submit" value="Сохранить"
                       class="btn btn-primary btn-lg btn-block"/>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}