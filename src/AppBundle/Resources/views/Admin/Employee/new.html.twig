{% extends '::base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="well">
                    <h2 class="text-primary text-center">Добавление нового сотрудника</h2>
                </div>

                {{ form_start(form) }}
                {{ form_widget(form) }}

                <input type="submit" value="Сохранить" class="btn btn-primary btn-lg btn-block"/>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}